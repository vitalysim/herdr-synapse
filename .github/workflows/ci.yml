name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: unittest (${{ matrix.os }}, python ${{ matrix.python }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest]
        python: ["3.9", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - name: Run the suite
        run: python -m unittest discover -s tests
      - name: Launcher and manifest sanity
        run: |
          sh -n bin/herdr-team bin/hook bin/herdr-team-sandbox
          ./bin/herdr-team --version
          ./bin/herdr-team --skill | head -n 3
